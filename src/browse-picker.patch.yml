# 固定使用 browse 目录选择器（跨平台稳定）
# 说明：patch 的 name 字段是"校验匹配"用的（必须与现有 entry 同名，否则被跳过），
# 所以必须先 disabled 旧的 auto 后端，再用 insert 挂载 browse 后端。
- id: directory-picker
  disabled: true
- insert:
    - id: directory-picker-browse
      name: '@deepseek-ai/dsh-host-directory-picker-browse'
